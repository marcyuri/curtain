// Évite les chaînes magiques dans les routes (Document 05 Partie IX,
// déjà appliqué côté Frontend — même discipline ici). Reprend
// exactement les clés du catalogue seedé (prisma/seed/seedPermissions.js).

export const PERMISSIONS = {
    PERMISSION_READ: "permission.read",
};
